{
  "name": "videosearch-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "electron/main/index.js",
  "scripts": {
    "electron": "electron .",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "uv run --directory backend pyinstaller --onefile --name backend --distpath backend/dist backend/entry.py",
    "dist:dir": "npm run build:frontend && npm run build:backend && electron-builder --win dir",
    "dist:win": "npm run build:frontend && npm run build:backend && electron-builder --win"
  },
  "devDependencies": {
    "electron": "^43.2.0",
    "electron-builder": "^26.15.3"
  },
  "build": {
    "appId": "com.videosearch.desktop",
    "productName": "VideoSearch",
    "directories": {
      "output": "release"
    },
    "files": [
      "package.json",
      "electron/**/*"
    ],
    "extraResources": [
      {
        "from": "backend/dist/backend.exe",
        "to": "backend/backend.exe"
      },
      {
        "from": "frontend/dist",
        "to": "frontend/dist",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "resources",
        "to": "resources",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "target": [
        "dir",
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "VideoSearch"
    }
  }
}
